1. For comments used an extension named "better comments" to highlight some comments in this project in different colors that indicate intensity 
2. npm packages in front-end:
 firebase,
 react-router-dom,
 react-multi-carousel
3. npm packages in back-end:
    nodemon, mongoose, mongodb, jsonwebtoken(JWT), firebase-admin, express, dotenv, cors, bcrypt
4. I have implemented separate logic to generate & provide refreshToken for google login firebase and email login(mongoDB storage). but the token verification is in the same file
5. 



{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/src/routes/api/$1" },
    {
      "source": "/register",
      "destination": "/src/controllers/registerController.js"
    },
    {
      "source": "/login",
      "destination": "/src/controllers/loginController.js"
    },
    {
      "source": "/passwordReset",
      "destination": "/src/controllers/forgotPasswordController.js"
    },
    {
      "source": "/jsonWebAccessToken",
      "destination": "/src/controllers/tokenGeneratorController.js"
    },
    { "source": "/(.*)", "destination": "/src/index.js" }
  ]
}
